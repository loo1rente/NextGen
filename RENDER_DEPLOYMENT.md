# Развернуть NextGen Messenger на Render

## Шаг 1: Создать PostgreSQL базу на Render

1. Зайди на [render.com](https://render.com) и залогинься
2. Нажми **"New+"** в левом меню → **"PostgreSQL"**
3. Заполни форму:
   - **Name**: `nextgen-messenger-db` (или любое имя)
   - **Database**: `nextgen_messenger` (или оставь как есть)
   - **Username**: `postgres` (по умолчанию)
   - **Region**: Выбери ближайший регион
   - **Plan**: Выбери бесплатный **Free** (если первый раз) или нужный план
4. Нажми **"Create Database"**
5. **ВАЖНО**: Когда база создана, скопируй полный **Internal Database URL** (внизу страницы):
   - Выглядит так: `postgresql://user:password@hostname:5432/dbname`
   - Сохрани этот URL - он понадобится!

## Шаг 2: Развернуть приложение на Render

1. Зайди в **Dashboard** → **"New+"** → **"Web Service"**
2. Выбери **"Connect a repository"** и выбери свой GitHub репозиторий
3. Заполни форму:
   - **Name**: `nextgen-messenger` (или любое имя)
   - **Environment**: `Node`
   - **Build Command**: `npm run build`
   - **Start Command**: `npm start`
   - **Region**: Выбери тот же регион что и база данных (важно!)
   - **Plan**: Выбери подходящий план

4. Нажми **"Advanced"** (развернуть дополнительные параметры)

## Шаг 3: Добавить переменные окружения

В разделе **"Environment Variables"** добавь:

```
DATABASE_URL = postgresql://user:password@hostname:5432/dbname
SESSION_SECRET = любая_длинная_случайная_строка_например_abc123xyz789
NODE_ENV = production
```

**Где взять DATABASE_URL**: Скопируй из шага 1 (Internal Database URL от базы)

**SESSION_SECRET**: Придумай любую длинную строку, например:
```
super_secret_key_12345_abcdefgh_randomstring
```

## Шаг 4: Развернуть

1. Нажми **"Create Web Service"**
2. Render автоматически начнет процесс:
   - Скачает репозиторий
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит приложение (`npm start`)
3. Посмотри логи (вкладка **"Logs"**) - если всё хорошо, увидишь:
   ```
   Server running on port 3000
   ```

## Шаг 5: Готово!

Когда статус станет **"Live"** (зелёный), нажми на URL приложения (выглядит как `https://nextgen-messenger-xyz.onrender.com`) - и твой мессенджер работает! 🎉

---

## Важные замечания:

⚠️ **Первый запуск**:
- База данных автоматически создаст все нужные таблицы при первом подключении
- Ничего вручную создавать не нужно!

⚠️ **Бесплатный план**:
- Приложение "засыпает" после 15 минут неактивности
- При первом обращении после спячки может быть задержка ~30 сек
- База данных может быть удалена через 90 дней без активности

⚠️ **Платные планы**:
- Доступны на Render и дают бесплатные кредиты $10/месяц для новых пользователей

⚠️ **WebSocket**:
- Real-time функции (сообщения, звонки, typing indicator) работают через WebSocket
- Render полностью поддерживает WebSocket - никаких проблем!

---

## Если что-то не работает:

1. **Проверь логи приложения** (вкладка "Logs" в Render)
2. **Проверь DATABASE_URL**: Правильно ли скопировал из Internal URL?
3. **Проверь SESSION_SECRET**: Заполнил ли поле?
4. **Перезагрузи** приложение (кнопка "Restart" в Dashboard)

---

## Обновления кода:

Когда ты сделаешь изменения в коде:
1. Закоммитай их в GitHub
2. Render автоматически заметит изменения
3. Нажмёт **"Manual Deploy"** если нужно, или просто подождёшь автоматического деплоя
4. Приложение обновится! ✅
